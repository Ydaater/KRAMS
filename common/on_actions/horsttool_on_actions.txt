on_actions = {
	on_startup = {
		effect = {
			if = {
				limit = {has_game_rule = {rule = select_host_country option = GER}}
				set_variable={host = GER.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = AST}}
				set_variable={host = AST.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = AUS}}
				set_variable={host = AUS.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = BRA}}
				set_variable={host = BRA.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = GEA}}
				set_variable={host = GEA.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = RAJ}}
				set_variable={host = RAJ.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = UKR}}
				set_variable={host = UKR.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = CAN}}
				set_variable={host = CAN.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = FRA}}
				set_variable={host = FRA.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = GER}}
				set_variable={host = GER.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = IRE}}
				set_variable={host = IRE.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = SRI}}
				set_variable={host = SRI.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = JAP}}
				set_variable={host = JAP.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = FNG}}
				set_variable={host = FNG.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = MEX}}
				set_variable={host = MEX.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = NOR}}
				set_variable={host = NOR.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = POL}}
				set_variable={host = POL.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = SIA}}
				set_variable={host = SIA.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = RUS}}
				set_variable={host = RUS.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = SWF}}
				set_variable={host = SWF.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = SWE}}
				set_variable={host = SWE.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = SWI}}
				set_variable={host = SWI.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = TUR}}
				set_variable={host = TUR.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = ENG}}
				set_variable={host = ENG.id}
			}
			else_if = {
				limit = {has_game_rule = {rule = select_host_country option = USA}}
				set_variable={host = USA.id}
			}
			var:host = {
				set_variable = { marked_states = 0 }
				set_country_flag = host
			}
		}
	}
}

